<!DOCTYPE html>
<html lang="hu">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Beach Barbershop</title>

    <style>
      * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        color: #f5f5f5;
        line-height: 1.6;
        overflow-x: hidden;
        position: relative;
        z-index: 0;
      }

      /* 🎥 Videó háttér */
      #hatter_video {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        object-fit: cover;
        z-index: -2;
      }

      /* 🌫️ Sötét overlay */
      body::before {
        content: "";
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.6);
        z-index: -1;
      }

      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1rem 2rem;
        background-color: rgba(17, 17, 17, 0.8);
        border-bottom: 2px solid #3cbcc7;
      }

      header .logo {
        font-size: 1.5rem;
        font-weight: bold;
        color: #3cbcc7;
      }

      header nav ul {
        list-style: none;
        display: flex;
        gap: 1rem;
      }

      header nav a {
        text-decoration: none;
        color: #f5f5f5;
        font-weight: 500;
        transition: color 0.3s;
      }

      header nav a:hover {
        color: #3cbcc7;
      }

      /* Bejelentkezés */
      section {
        padding: 4rem 2rem;
        background: rgba(0, 0, 0, 0.4);
        border-radius: 10px;
        margin: 13rem auto;
        max-width: 400px;
        text-align: center;
      }

      h2 {
        font-size: 2rem;
        margin-bottom: 2rem;
        color: #3cbcc7;
      }

      form {
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      input,
      button {
        padding: 0.8rem;
        border-radius: 5px;
        border: none;
        outline: none;
      }

      input {
        background-color: #333;
        color: #f5f5f5;
      }

      button {
        background-color: #3cbcc7;
        color: #111;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.3s;
      }

      button:hover {
        background-color: #34aab4;
      }

      footer {
        text-align: center;
        padding: 2rem;
        background-color: rgba(17, 17, 17, 0.8);
        border-top: 2px solid #3cbcc7;
        color: #ccc;
      }

      .regisztracio-link {
        margin-top: 1rem;
        font-size: 0.9rem;
        color: #ccc;
      }

      .regisztracio-link a {
        color: #3cbcc7;
        text-decoration: none;
        font-weight: bold;
      }

      .regisztracio-link a:hover {
        text-decoration: underline;
      }
    </style>
  </head>
  <body>
    <!-- Videó háttér -->
    <video id="hatter_video" autoplay muted loop>
      <source src="/videos/video.mp4" type="video/mp4" />
      A böngésződ nem támogatja a videó elemet.
    </video>

    <header>
      <div class="logo">Beach Barbershop</div>
      <nav>
        <ul>
          <li><a href="/">Vissza a főoldalra</a></li>
        </ul>
      </nav>
    </header>

    <section id="register">
      <h2>Regisztráció</h2>
      <form id="regisztracio_urlap" action="/api/register" method="post">
        <input type="text" name="username" placeholder="Teljes név" required />
        <input type="email" name="email" placeholder="Email cim" required />
        <input type="text" name="pnumber" placeholder="Telefonszám" required />
        <input type="password" name="password" placeholder="Jelszó" required />
        <input type="password" placeholder="Jelszó megerősítése" required />
        <button type="submit">Regisztráció</button>

        <p class="regisztracio-link">Már van fiókod? <a href="/bejelentkezes">Jelentkezz be!</a></p>
      </form>
    </section>

    <footer>
      <p>&copy; 2025 Beach Barbershop</p>
    </footer>

    <script>
      document.getElementById("bejelentkezes_urlap").addEventListener("submit", function (e) {
        e.preventDefault();
        alert("Bejelentkezés sikeres (demo)!");
      });
    </script>
  </body>
</html>
